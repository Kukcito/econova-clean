export default function Radar4(){
  return (
    <section className="section" id="radar4">
      <div className="container">
        <h2 className="h2">Radar4: decisiones basadas en datos</h2>
        <p className="lead">
          Visualiza desempeño por ejes (ambiental, social, seguridad, gobernanza) para priorizar acciones y comunicar avances.
        </p>
        <div className="card" style={{minHeight:180, display:'grid', placeItems:'center'}}>
          <span style={{color:'#64748b'}}>Aquí podemos incrustar un gráfico/imagen del radar (SVG o PNG) y KPIs dinámicos.</span>
        </div>
      </div>
    </section>
  );
}
